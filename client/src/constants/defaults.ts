import { TGroup, TItem } from '../types/TTransaction';

export const defaultTransactionIndex = 0;
export const defaultModalType = '';
export const defaultModalFunction = '';
export const defaultTransactionItem = {} as TItem;
export const defaultTransaction = {} as TGroup;
export const defaultInputString = '';
export const defaultInputNumber = 0;
